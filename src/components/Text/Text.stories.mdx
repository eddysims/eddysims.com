import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { Text } from ".";

<Meta
  title="components/Text"
  component={Text}
  parameters={{
    viewMode: "docs",
  }}
/>

# Text

`Text` is pretty self explanitory. It is used for paragraphs of text.

## Usage

```ts
import { Text } from "@/components/Text";
```

## Props

<ArgsTable of={Text} />

## Example

<Canvas>
  <Story name="Default">
    {(args) => <Text {...args}>This is some text content.</Text>}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Small Text

The `Text` component can be used with small text as well.

<Canvas>
  <Story name="Small Text" args={{ size: "small" }}>
    {(args) => <Text {...args}>This is some small text content.</Text>}
  </Story>
</Canvas>

<ArgsTable story="Small Text" />
