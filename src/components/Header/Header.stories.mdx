import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { Header } from ".";

<Meta
  title="layout/Header"
  component={ Header }
  parameters={{
    viewMode: "docs",
  }}
/>

# Header

The `Header` is used to display the navigation and header for the website.

## Usage

```ts
import { Header } from "@/components/Header";
```

## Props

<ArgsTable of={ Header } />

## Example

<Canvas>
  <Story name="Default">{(args) => <Header {...args} />}</Story>
</Canvas>
