import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { Layout } from ".";

<Meta title="layouts/Layout" component={Layout} />

# Layout

The `Layout` is main layout for the website. All other layout components should be based off of this component.

## Usage

```tsx
import { Layout } from "@/components/Layout";
```

## Props

<ArgsTable component={Layout} />

## Example

<Canvas>
  <Story name="Default">{(args) => <Layout {...args}>This is some content</Layout>}</Story>
</Canvas>
