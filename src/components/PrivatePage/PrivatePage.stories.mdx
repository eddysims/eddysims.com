import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { LayoutCentered } from "@/components/LayoutCentered";
import { PrivatePage } from ".";

<Meta
  title="components/PrivatePage"
  component={PrivatePage}
  parameters={{
    viewMode: "docs",
  }}
/>

# PrivatePage

The `PrivatePage` component is used to hide content from users that are not allowed to view the page content.

## Usage

```ts
import { PrivatePage } from "@/components/PrivatePage";
```

## Props

<ArgsTable of={PrivatePage} />

## Example

<Canvas>
  <Story name="Default">
    {(args) => (
      <PrivatePage {...args}>
        <LayoutCentered>🤫 Shh, this is some private content</LayoutCentered>
      </PrivatePage>
    )}
  </Story>
</Canvas>

<ArgsTable story="Default" />
