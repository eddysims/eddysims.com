import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { Hero } from ".";

<Meta
  title="components/Hero"
  component={ Hero }
  parameters={{
    viewMode: "docs",
  }}
/>

# Hero

A `Hero` is used to display a large block of isolated content. Most often used for page headers or featured pieces of content.

## Usage

```ts
import { Hero } from "@/components/Hero";
```

## Props

<ArgsTable of={ Hero } />

## Example

<Canvas>
  <Story name="Default">{(args) => <Hero {...args}>I am my own biggest hero</Hero>}</Story>
</Canvas>
