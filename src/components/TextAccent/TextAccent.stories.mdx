import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { TextAccent } from ".";

<Meta
  title="components/TextAccent"
  component={TextAccent}
  parameters={{
    viewMode: "docs",
  }}
/>

# TextAccent

The `TextAccent` component is used to highlight, or "accent" text.

## Usage

```ts
import { TextAccent } from "@/components/TextAccent";
```

## Props

<ArgsTable of={TextAccent} />

## Example

<Canvas>
  <Story name="Default">
    {(args) => (
      <div>
        This is some text that needs an{" "}
        <TextAccent {...args}>accent</TextAccent>
      </div>
    )}
  </Story>
</Canvas>
