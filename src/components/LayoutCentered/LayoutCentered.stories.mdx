import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { LayoutCentered } from ".";

<Meta
  title="layouts/LayoutCentered"
  component={LayoutCentered}
  parameters={{
    viewMode: "docs",
  }}
/>

# LayoutCentered

`LayoutCentered` will extend the `Layout` component and center all of the content in the middle of the viewport.

## Usage

```ts
import { LayoutCentered } from "@/components/LayoutCentered";
```

## Props

<ArgsTable of={LayoutCentered} />

## Example

<Canvas>
  <Story name="Default">
    {(args) => <LayoutCentered {...args}>Some centered content</LayoutCentered>}
  </Story>
</Canvas>
