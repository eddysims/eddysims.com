import { Meta } from "@storybook/blocks";

<Meta title="hooks/useForm" />

# useForm

The `useForm` is a wrapper around the `react-hook-form` useForm hook. This hook will expose only what is needed for our application.

## Usage

```tsx
import { useForm } from "@/hooks/useForm";
```
